name: "Composite Action Test Errors"
description: "Run a simple composite action"
env:
  NAME2: test2
  SERVER: development
inputs:
  your_name:
    description: 'lol'
    default: 'Ethan'
outputs:
  random_number: ${{ fromJson('not json') }}
  food: ${{ steps.food.outputs.test }}
runs:
    using: "composite"
    steps: 
        - run: ecdsfho hello
